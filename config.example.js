/**
 * Template for config.js. Copy it, fill in ONE of the two routes, and commit.
 * There are no API keys here and there never will be.
 */

// Route 1 - published CSV (recommended, keeps the document private).
export const PRICING_CSV_URL = '';
export const RULES_CSV_URL = '';

// Route 2 - link-shared document, read through the gviz CSV endpoint.
// The long id from https://docs.google.com/spreadsheets/d/<THIS PART>/edit
export const SHEET_ID = '';
